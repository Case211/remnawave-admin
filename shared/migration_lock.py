"""Ключ блокировки, под которой идут миграции схемы.

Панель, коллектор и бот поднимаются одновременно и делят одну базу. Каждый
на время апгрейда убирает из ``alembic_version`` плагинные ветки (их wheel
может быть ещё не установлен) и возвращает обратно — а пока один планирует
путь, другой уже вернул чужую ревизию. Итог: «Can't locate revision
identified by ...» и старт с ограниченной функциональностью.

Advisory lock выстраивает их в очередь. Тот, кто дождался, видит схему уже
применённой и просто выходит. Ключ обязан совпадать у всех трёх — иначе
блокировки будут разные и толку от них никакого.
"""

MIGRATION_LOCK_KEY = 8_150_017_001
